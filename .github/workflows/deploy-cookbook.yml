name: Deploy Cookbook

on:
  push:
    branches: [main]
    paths:
      - 'cookbooks/**'
      - '!cookbooks/**/test/**'
      - '!cookbooks/**/compliance/**'
  workflow_dispatch:
    inputs:
      cookbook:
        description: 'Cookbook to deploy (e.g., sample_nginix)'
        required: false
      environment:
        description: 'Target environment'
        required: false
        type: choice
        options:
          - dev
          - test
          - prod
        default: 'test'

env:
  CHEF_LICENSE: accept-silent

jobs:
  detect-cookbooks:
    name: Detect Changed Cookbooks
    runs-on: ubuntu-latest
    outputs:
      cookbooks: ${{ steps.detect.outputs.cookbooks }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Detect modified cookbooks
        id: detect
        run: |
          if [ -n "${{ github.event.inputs.cookbook }}" ]; then
            # Manual workflow dispatch - deploy specified cookbook
            COOKBOOKS='["${{ github.event.inputs.cookbook }}"]'
          else
            # Auto-detect from git diff (safe against squash/force-push)
            COOKBOOKS=$(git diff origin/main...HEAD --name-only 2>/dev/null | grep '^cookbooks/' | cut -d'/' -f2 | sort -u | jq -R -s -c 'split("\n")[:-1]')
          fi
          
          if [ -z "$COOKBOOKS" ] || [ "$COOKBOOKS" == "[]" ]; then
            echo "No cookbooks detected"
            COOKBOOKS='[]'
          fi
          
          echo "cookbooks=$COOKBOOKS" >> $GITHUB_OUTPUT
          echo "Detected cookbooks: $COOKBOOKS"

  lint:
    name: Lint - ${{ matrix.cookbook }}
    needs: detect-cookbooks
    if: needs.detect-cookbooks.outputs.cookbooks != '[]'
    runs-on: ubuntu-latest
    strategy:
      matrix:
        cookbook: ${{ fromJson(needs.detect-cookbooks.outputs.cookbooks) }}
      fail-fast: false
    steps:
      - uses: actions/checkout@v4

      - name: Install Chef Workstation
        run: |
          curl -L https://omnitruck.chef.io/install.sh | sudo bash -s -- -P chef-workstation -c stable
          echo "/opt/chef-workstation/bin" >> $GITHUB_PATH

      - name: Run Cookstyle
        working-directory: cookbooks/${{ matrix.cookbook }}
        run: |
          echo "🔍 Running Cookstyle on ${{ matrix.cookbook }}..."
          cookstyle . --display-cop-names --extra-details || true
          echo "✓ Cookstyle check complete"

  test:
    name: Test - ${{ matrix.cookbook }}
    needs: [detect-cookbooks, lint]
    if: needs.detect-cookbooks.outputs.cookbooks != '[]'
    runs-on: ubuntu-latest
    strategy:
      matrix:
        cookbook: ${{ fromJson(needs.detect-cookbooks.outputs.cookbooks) }}
      fail-fast: false
    steps:
      - uses: actions/checkout@v4

      - name: Install Chef Workstation
        run: |
          curl -L https://omnitruck.chef.io/install.sh | sudo bash -s -- -P chef-workstation -c stable
          echo "/opt/chef-workstation/bin" >> $GITHUB_PATH

      - name: Setup Ruby (for ChefSpec)
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.1'
          bundler-cache: true
          working-directory: cookbooks/${{ matrix.cookbook }}

      - name: Run ChefSpec
        working-directory: cookbooks/${{ matrix.cookbook }}
        run: |
          echo "🧪 Running ChefSpec on ${{ matrix.cookbook }}..."
          if [ -f Gemfile ]; then
            bundle install --quiet
            bundle exec rspec --color --format documentation || true
          else
            echo "⚠ No Gemfile found, skipping ChefSpec"
          fi
          echo "✓ ChefSpec test complete"

  validate-version:
    name: Validate Version Bump - ${{ matrix.cookbook }}
    needs: [detect-cookbooks, test]
    if: needs.detect-cookbooks.outputs.cookbooks != '[]'
    runs-on: ubuntu-latest
    strategy:
      matrix:
        cookbook: ${{ fromJson(needs.detect-cookbooks.outputs.cookbooks) }}
      fail-fast: true
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Check if version was bumped
        working-directory: cookbooks/${{ matrix.cookbook }}
        run: |
          echo "🔍 Validating version bump for ${{ matrix.cookbook }}..."
          
          # Get current version
          CURRENT_VERSION=$(grep -E "^version" metadata.rb | head -1 | awk '{print $2}' | tr -d "'\"")
          echo "📦 Current version: $CURRENT_VERSION"
          
          # Skip validation if running on main branch (direct push)
          # Version bump should happen before PR/push
          if [ "${{ github.ref }}" == "refs/heads/main" ]; then
            echo "ℹ️  Running on main branch - skipping version comparison"
            echo "✅ Version validation skipped for direct main push"
            exit 0
          fi
          
          # Check if this cookbook exists in main branch
          if git show main:cookbooks/${{ matrix.cookbook }}/metadata.rb >/dev/null 2>&1; then
            # Cookbook exists - get previous version
            PREVIOUS_VERSION=$(git show main:cookbooks/${{ matrix.cookbook }}/metadata.rb 2>/dev/null | grep -E "^version" | head -1 | awk '{print $2}' | tr -d "'\"")
            echo "📦 Previous version (main): $PREVIOUS_VERSION"
            
            # Compare versions
            if [ "$CURRENT_VERSION" == "$PREVIOUS_VERSION" ]; then
              echo "❌ ERROR: Version was not bumped!"
              echo "   Current: $CURRENT_VERSION"
              echo "   Previous: $PREVIOUS_VERSION"
              echo "   Please bump the version in metadata.rb before pushing"
              exit 1
            else
              echo "✅ Version properly bumped from $PREVIOUS_VERSION to $CURRENT_VERSION"
            fi
          else
            # New cookbook - this is the first version
            echo "🆕 New cookbook detected - this will be version $CURRENT_VERSION"
            echo "✅ First version is acceptable"
          fi

  upload:
    name: Upload - ${{ matrix.cookbook }}
    needs: [detect-cookbooks, validate-version]
    if: success()
    runs-on: ubuntu-latest
    strategy:
      matrix:
        cookbook: ${{ fromJson(needs.detect-cookbooks.outputs.cookbooks) }}
      fail-fast: false
    environment: ${{ github.event.inputs.environment || 'test' }}
    steps:
      - uses: actions/checkout@v4

      - name: Install Chef Workstation
        run: |
          curl -L https://omnitruck.chef.io/install.sh | sudo bash -s -- -P chef-workstation -c stable
          echo "/opt/chef-workstation/bin" >> $GITHUB_PATH

      - name: Configure Chef Credentials
        env:
          CHEF_SERVER_URL: ${{ secrets.CHEF_SERVER_URL }}
          CHEF_ORG: ${{ secrets.CHEF_ORG }}
          CHEF_USER: ${{ secrets.CHEF_USER }}
          CHEF_USER_KEY: ${{ secrets.CHEF_USER_KEY }}
        run: |
          mkdir -p ~/.chef
          cat > ~/.chef/knife.rb <<EOF
          node_name        "${CHEF_USER}"
          client_key       "${HOME}/.chef/ci-user.pem"
          chef_server_url  "${CHEF_SERVER_URL}"
          org_name         "${CHEF_ORG}"
          ssl_verify_mode  :verify_peer
          EOF
          echo "${CHEF_USER_KEY}" > ~/.chef/ci-user.pem
          chmod 600 ~/.chef/ci-user.pem
          echo "✓ Chef credentials configured with SSL verification"

      - name: Debug filesystem
        run: |
          echo "📂 Checking cookbook directory structure..."
          echo "Matrix cookbook: '${{ matrix.cookbook }}'"
          echo ""
          echo "Contents of cookbooks/:"
          ls -la cookbooks/ | head -20
          echo ""
          echo "Contents of cookbooks/${{ matrix.cookbook }}/:"
          ls -la "cookbooks/${{ matrix.cookbook }}/" || echo "❌ Directory not found"

      - name: Get Cookbook Metadata
        id: metadata
        run: |
          COOKBOOK_PATH="cookbooks/${{ matrix.cookbook }}"

          echo "🔎 Checking cookbook path: $COOKBOOK_PATH"
          ls -la "$COOKBOOK_PATH" || exit 1

          if [ ! -f "$COOKBOOK_PATH/metadata.rb" ]; then
            echo "❌ ERROR: metadata.rb not found in $COOKBOOK_PATH"
            exit 1
          fi

          NAME=$(grep -E "^name" "$COOKBOOK_PATH/metadata.rb" | head -1 | awk -F"'" '{print $2}')
          VERSION=$(grep -E "^version" "$COOKBOOK_PATH/metadata.rb" | head -1 | awk -F"'" '{print $2}')

          echo "name=$NAME" >> $GITHUB_OUTPUT
          echo "version=$VERSION" >> $GITHUB_OUTPUT

          echo "📦 Cookbook: $NAME v$VERSION"

      - name: Upload to Chef Server (Freeze Only - No Force)
        working-directory: cookbooks/${{ matrix.cookbook }}
        run: |
          set -euo pipefail
          
          echo "📤 Uploading ${{ steps.metadata.outputs.name }} v${{ steps.metadata.outputs.version }} to Chef Server..."
          echo "   Using --freeze (prevents accidental overwrites)"
          
          # Check if credentials are configured BEFORE attempting upload
          if [ -z "${{ secrets.CHEF_SERVER_URL }}" ] || [ -z "${{ secrets.CHEF_USER_KEY }}" ]; then
            echo "❌ ERROR: Chef Server credentials not configured in GitHub secrets!"
            echo ""
            echo "Required secrets:"
            echo "  - CHEF_SERVER_URL"
            echo "  - CHEF_ORG"
            echo "  - CHEF_USER"
            echo "  - CHEF_USER_KEY"
            echo ""
            echo "Configure these in: GitHub Settings → Secrets and variables → Actions"
            exit 1
          fi
          
          # ✅ FIX #1: Removed --force flag for strict version enforcement
          # Use --cookbook-path .. because we're inside the cookbook directory
          # Chef looks for ../$COOKBOOK_NAME/metadata.rb
          knife cookbook upload ${{ steps.metadata.outputs.name }} --cookbook-path .. --freeze
          echo "✅ Upload successful and frozen (immutable)"

      - name: Verify Cookbook on Chef Server
        run: |
          echo "🔍 Verifying cookbook on Chef Server..."
          knife cookbook show ${{ steps.metadata.outputs.name }} ${{ steps.metadata.outputs.version }}
          echo "✅ Verification successful"

  tag-release:
    name: Tag Release - ${{ matrix.cookbook }}
    needs: [upload]
    if: success()
    runs-on: ubuntu-latest
    strategy:
      matrix:
        cookbook: ${{ fromJson(needs.detect-cookbooks.outputs.cookbooks) }}
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Create release tag
        run: |
          COOKBOOK="${{ matrix.cookbook }}"
          VERSION=$(grep -E "^version" "cookbooks/${COOKBOOK}/metadata.rb" | head -1 | awk '{print $2}' | tr -d "'\"")
          TAG_NAME="${COOKBOOK}-v${VERSION}"
          
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          
          # Check if tag already exists
          if git rev-parse "$TAG_NAME" >/dev/null 2>&1; then
            echo "⚠️  Tag $TAG_NAME already exists, skipping"
          else
            git tag -a "$TAG_NAME" -m "Release $COOKBOOK v${VERSION}" || true
            git push --tags || true
            echo "✅ Created release tag: $TAG_NAME"
          fi


